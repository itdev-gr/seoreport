---
import Layout from '../../layouts/Layout.astro';
import { mockClients } from '../../data/mock-clients';
---

<Layout title="Admin - SEO Report - IT DEV" showHeader={true}>
  <div slot="header-actions">
    <a
      href="/dashboard"
      class="rounded-lg bg-white/20 px-4 py-2.5 text-sm font-medium text-white transition hover:bg-white/30 focus:outline-none focus:ring-2 focus:ring-white/50"
    >
      ← Dashboard
    </a>
  </div>

  <div class="rounded-xl border border-gray-200 bg-white shadow-card overflow-hidden">
    <div class="border-b border-gray-100 px-6 py-4">
      <h1 class="text-xl font-semibold text-dark">Admin Panel</h1>
      <p class="mt-0.5 text-sm text-gray-500">Επιλέξτε πελάτη για να δείτε το report. Μπορείτε να το ανοίξετε και να το δείτε παρέα με τον πελάτη (ίδιο link).</p>
    </div>
    <ul class="divide-y divide-gray-100">
      {mockClients.map((client) => (
        <li class="flex items-center justify-between gap-4 px-6 py-4 hover:bg-gray-50/50 transition">
          <div>
            <p class="font-medium text-dark">{client.name}</p>
            <p class="text-sm text-gray-500">{client.domain}</p>
          </div>
          <a
            href={`/report/${client.id}`}
            class="shrink-0 rounded-lg bg-primary px-4 py-2.5 text-sm font-medium text-white transition hover:bg-primary/90 focus:outline-none focus:ring-2 focus:ring-primary focus:ring-offset-2"
          >
            Προβολή report
          </a>
        </li>
      ))}
    </ul>
  </div>
</Layout>
